spring:
  application:
    name: saboteur

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:

socketio:
  server:
    hostname:
    port:

logging:
  level:
    org.springframework.boot.autoconfigure: error